<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="generator" content="{{generator}}">
  <title>{{slot:title}} - {{site_name}}</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      line-height: 1.6;
      color: #333;
    }
    header {
      background: {{theme_color}};
      color: white;
      padding: 2rem;
      text-align: center;
    }
    nav {
      background: #f8f9fa;
      padding: 1rem;
      text-align: center;
    }
    nav a {
      margin: 0 1rem;
      color: {{theme_color}};
      text-decoration: none;
    }
    main {
      max-width: 800px;
      margin: 2rem auto;
      padding: 0 2rem;
    }
    footer {
      background: #f8f9fa;
      padding: 2rem;
      text-align: center;
      margin-top: 4rem;
    }
    .meta {
      font-size: 0.875rem;
      color: #666;
      margin-top: 1rem;
    }
  </style>
  {{slot:extra_head}}
</head>
<body>
  <header>
    <h1>{{site_name}}</h1>
    <p>{{description}}</p>
  </header>
  
  {{> _navigation}}
  
  <main>
    {{slot:content}}
  </main>
  
  {{> _footer}}
</body>
</html>
